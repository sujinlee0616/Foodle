<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.retaurant-themalist-mapper">
<!-- t_MainThema= #{menuName} -->
	<select id="cateSelectData" resultType="MainThemaVO" parameterType="java.util.Map">
		SELECT t_MainThema, t_DetailThema, t_Info
		FROM mainThema
		WHERE REGEXP_LIKE(t_MainThema,#{fd})
</select>	

<!-- 
 스몰   테마 개수를 알아오기   위함!! 
<select id="smallThemaCount" resultType="int" parameterType="java.util.Map">
	    SELECT COUNT(*)
		FROM detailThema
		WHERE t_DetailThema=#{t_DetailThema}
        GROUP BY t_detailthema
</select>
 			-->



<!-- 모든 정보 가져오기! -->
<select id="showMoreCate" resultType="DetailThemaVO" parameterType="java.util.Map">
	SELECT t_DetailThema, t_Info
	FROM detailThema 
</select>
</mapper>