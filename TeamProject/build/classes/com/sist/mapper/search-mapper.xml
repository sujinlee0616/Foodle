<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.mapper.search-mapper">
  <select id="searchListData" resultType="MaininfoVO" parameterType="java.util.Map">
    SELECT rno,rname,rtype,rscore,rscorecount,rarea,raddr1,rtel,num 
    FROM (SELECT rno,rname,rtype,rscore,rscorecount,rarea,raddr1,rtel,rownum as num 
    FROM (SELECT rno,rname,rtype,rscore,rscorecount,rarea,raddr1,rtel 
    FROM maininfo ORDER BY rno ASC)) 
    WHERE num BETWEEN #{start} AND #{end}
  </select>
  
  <select id="searchTotalPage" resultType="int">
  	SELECT CEIL(COUNT(*)/20.0) FROM maininfo
  </select>
</mapper>